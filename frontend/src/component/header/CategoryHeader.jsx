import React from 'react';

const CategoryHeader = ({CategoryHeader}) => {
    return (

              <li><a href="#">{CategoryHeader.name}</a></li>
    );
};

export default CategoryHeader;